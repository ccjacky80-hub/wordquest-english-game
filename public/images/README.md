# Vocabulary images

Use the stable vocabulary ID as the runtime asset key:

```text
public/images/C0012.webp
```

Keep generated source files and review metadata outside the runtime path. The manifest reference belongs in `VocabularyEntry.media.image`.
