# Vocabulary audio

Use the stable vocabulary ID and semantic suffix:

```text
public/audio/C0012-word.mp3
public/audio/C0012-slow.mp3
```

Audio provider and attribution metadata must be tracked before an asset is referenced by runtime content.
